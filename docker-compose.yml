services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: "123lomp098rnhD"
      MYSQL_DATABASE: InkLink_db
      MYSQL_USER: InkLink
      MYSQL_PASSWORD: Inklink2026
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: